// import { selectMyAdminTasksDomain } from '../selectors';

describe('selectMyAdminTasksDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
